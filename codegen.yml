overwrite: true
schema: 'http://localhost:8000/graphql'
generates:
  src/gql/types/index.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  
  src/gql/types/graphql.schema.json: 
    plugins: 
      - 'introspection'